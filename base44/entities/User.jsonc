{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "account_type": {
      "type": "string",
      "description": "Whether the user signed up as a creator or a brand",
      "enum": [
        "creator",
        "brand"
      ],
      "default": "creator"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}